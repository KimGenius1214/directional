# 📝 Directional - 게시판 & 차트 대시보드

게시글 CRUD 기능과 다양한 데이터 시각화 차트를 제공하는 현대적인 풀스택 웹 애플리케이션입니다.

## ✨ 주요 기능

### 1️⃣ 인증 시스템

- ✅ JWT 기반 로그인/로그아웃
- ✅ 쿠키 기반 세션 관리
- ✅ 미들웨어를 통한 라우트 보호
- ✅ 자동 토큰 갱신
- ✅ 인증 상태 지속성

### 2️⃣ 게시판

- ✅ 게시글 작성, 조회, 수정, 삭제 (CRUD)
- ✅ **반응형 디자인**
  - 데스크톱: 테이블 뷰 (컬럼 리사이징, 정렬)
  - 모바일: 카드 뷰 (터치 친화적)
- ✅ 무한 스크롤 페이지네이션
- ✅ 실시간 검색 (제목, 본문)
- ✅ 카테고리 필터링 (공지, Q&A, 자유)
- ✅ 컬럼 가시성 토글 (눈 아이콘)
- ✅ 정렬 기능 (오름차순/내림차순)
- ✅ Mock 데이터 모드 (개발/테스트용)

### 3️⃣ 데이터 시각화

- ✅ **다양한 차트 타입**
  - 심플 바 차트 & 도넛 차트
  - 스택형 바 차트 & 면적 차트
  - 멀티라인 차트 (듀얼 Y축, 커스텀 마커)
- ✅ 인터랙티브 범례
  - 클릭으로 데이터 숨김/보임
  - 색상 피커로 실시간 색상 변경
  - 상태 지속성 (Zustand)
- ✅ 커스텀 툴팁
  - 호버 시 상세 정보 표시
  - 부드러운 페이드인 애니메이션
  - 한글 레이블 지원

### 4️⃣ UI/UX

- ✅ 다크/라이트 모드 (아이폰 스타일 토글)
- ✅ 완전한 반응형 디자인
- ✅ Toast 알림 (Sonner)
- ✅ Error Boundary로 안정성 확보
- ✅ 로딩 상태 및 스켈레톤 UI
- ✅ 접근성 최적화

## 🛠️ 기술 스택

### 코어 프레임워크

- **React** 19.2.0 - UI 라이브러리
- **Next.js** 16.0.5 (App Router) - React 프레임워크
- **TypeScript** 5+ - 타입 안정성

### 스타일링

- **Tailwind CSS** 4 - 유틸리티 기반 CSS
- **Lucide React** - 아이콘 라이브러리

### 상태 관리

- **Zustand** - 클라이언트 상태 (인증, 테마, 차트 범례)
- **React Query (TanStack Query)** - 서버 상태 및 캐싱
- **Universal Cookie** - 쿠키 관리

### UI 컴포넌트

- **Recharts** - 데이터 시각화
- **TanStack Table** - 고성능 테이블
- **React Hook Form** - 폼 관리
- **Zod** - 스키마 검증
- **Sonner** - 토스트 알림
- **React Intersection Observer** - 무한 스크롤

### API & 네트워크

- **Axios** - HTTP 클라이언트
- **Next.js Middleware** - 라우트 보호 및 인증

## 📁 프로젝트 구조

```
directional/
├── app/                           # Next.js App Router
│   ├── page.tsx                  # 홈 페이지
│   ├── login/page.tsx            # 로그인 페이지
│   ├── posts/page.tsx            # 게시판 페이지
│   ├── dashboard/page.tsx        # 대시보드 페이지
│   ├── layout.tsx                # 루트 레이아웃
│   └── globals.css               # 전역 스타일
├── middleware.ts                  # Next.js 미들웨어 (인증)
├── src/
│   ├── components/               # 공통 컴포넌트
│   │   ├── ui/                  # UI 컴포넌트 (Button, Input, Card 등)
│   │   ├── layout/              # 레이아웃 (Header, Sidebar)
│   │   └── error/               # Error Boundary
│   ├── features/                 # 기능별 모듈 (Feature-First)
│   │   ├── auth/                # 인증 관련
│   │   ├── posts/               # 게시판 (컴포넌트, 훅)
│   │   │   ├── components/     # PostsTable, PostFormModal 등
│   │   │   └── hooks/          # use-posts 등
│   │   └── charts/              # 차트 (컴포넌트, 훅)
│   │       ├── components/     # 각종 차트 컴포넌트
│   │       └── hooks/          # 차트 데이터 및 범례 훅
│   ├── lib/                      # 라이브러리 및 설정
│   │   ├── api/                 # API 클라이언트 및 엔드포인트
│   │   ├── store/               # Zustand 스토어
│   │   ├── providers/           # React Context 프로바이더
│   │   └── utils/               # 유틸리티 함수
│   ├── types/                    # TypeScript 타입 정의
│   └── constants/                # 상수 (API URL, 색상, 검증 규칙)
├── public/                       # 정적 파일
├── ARCHITECTURE.md               # 아키텍쳐 상세 문서
├── DEVELOPMENT.md                # 개발 가이드
└── SETUP_COMPLETE.md             # 초기 설정 완료 체크리스트
```

자세한 아키텍쳐는 [ARCHITECTURE.md](./ARCHITECTURE.md)를 참고하세요.

## 🚀 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

### 3. API 정보

- **API Base URL**: https://fe-hiring-rest-api.vercel.app
- **API 문서**: https://fe-hiring-rest-api.vercel.app/docs

로그인에 필요한 이메일과 비밀번호는 API 문서를 참고하세요.

## 📖 사용 방법

### 1. 로그인

1. 홈 페이지에서 "로그인" 카드 클릭 또는 네비게이션에서 로그인 선택
2. API 문서에서 제공하는 이메일과 비밀번호 입력
   - API 문서: https://fe-hiring-rest-api.vercel.app/docs
3. 로그인 성공 시 토스트 알림과 함께 홈으로 이동
4. 헤더에 이메일 표시 및 로그아웃 버튼 활성화

### 2. 게시판

#### 게시글 관리

1. 네비게이션에서 "게시판" 클릭 (로그인 필요)
2. **새 게시글 작성**: "+ 새 게시글" 버튼 클릭
   - 카테고리, 제목, 내용, 태그 입력
   - 금칙어 검증 (바보, 멍청이, 욕설 등)
3. **게시글 수정**: 각 행의 "수정" 버튼 클릭
4. **게시글 삭제**: 각 행의 "삭제" 버튼 클릭 후 확인

#### 검색 및 필터링

- **검색**: 제목 또는 내용으로 실시간 검색
- **카테고리 필터**: 전체, 공지, Q&A, 자유 선택
- **정렬**: 컬럼 헤더 클릭으로 오름차순/내림차순 전환

#### 테이블 커스터마이징 (데스크톱)

- **컬럼 리사이징**: 컬럼 경계선 드래그
- **컬럼 숨김/보임**: 상단 "컬럼 표시" 섹션에서 눈 아이콘 클릭
- **무한 스크롤**: 하단까지 스크롤 시 자동으로 더 불러오기

#### 모바일 뷰

- 카드 형태로 게시글 표시
- 터치 친화적인 UI
- 모든 CRUD 기능 동일하게 지원

#### Mock 데이터 모드

- "목업 데이터 사용" 토글 활성화
- 실제 API 호출 없이 테스트 가능
- 클라이언트 사이드 페이지네이션

### 3. 대시보드

#### 차트 보기

1. 네비게이션에서 "대시보드" 클릭 (인증 불필요)
2. 다양한 차트로 데이터 시각화 확인
   - 커피 섭취량과 생산성 관계 (멀티라인)
   - 스낵 섭취량과 팀 사기 (멀티라인)
   - 기타 바/도넛/스택 차트

#### 차트 인터랙션

- **데이터 포인트 호버**: 툴팁으로 상세 정보 표시
- **범례 클릭**: 해당 데이터 숨김/보임 토글
- **범례 색상 변경**:
  1. 범례 항목의 색상 박스 클릭
  2. 색상 피커에서 원하는 색상 선택
  3. 변경 사항은 자동으로 저장됨

### 4. 테마 전환

- 헤더의 토글 버튼 (해/달 아이콘) 클릭
- 라이트 모드 ↔ 다크 모드 전환
- 선택한 테마는 로컬 스토리지에 저장

## 🎯 주요 기능 구현 상태

### ✅ 완료된 기능

#### 인프라 & 설정

- [x] Next.js 프로젝트 초기 설정
- [x] TypeScript 타입 시스템 구축
- [x] Tailwind CSS 설정 및 다크 모드
- [x] ESLint & Prettier 설정
- [x] 환경 변수 관리

#### 인증 시스템

- [x] JWT 기반 로그인/로그아웃
- [x] 쿠키 기반 세션 관리
- [x] Next.js 미들웨어 인증 보호
- [x] 자동 토큰 갱신
- [x] 로그인 페이지 UI

#### API & 상태 관리

- [x] Axios 클라이언트 설정
- [x] API 엔드포인트 모듈화
- [x] React Query 설정 및 캐싱
- [x] Zustand 스토어 (인증, 테마, 차트 범례)
- [x] Error Boundary 구현
- [x] Toast 알림 시스템

#### 게시판

- [x] 게시글 CRUD 전체 구현
- [x] TanStack Table 통합
- [x] 무한 스크롤 페이지네이션
- [x] 실시간 검색 기능
- [x] 카테고리 필터링
- [x] 컬럼 리사이징
- [x] 컬럼 가시성 토글
- [x] 정렬 기능
- [x] 금칙어 검증
- [x] Mock 데이터 모드
- [x] 모바일 카드 뷰
- [x] 반응형 디자인

#### 차트 대시보드

- [x] 심플 바 차트
- [x] 심플 도넛 차트
- [x] 스택형 바 차트
- [x] 스택형 면적 차트
- [x] 멀티라인 차트 (듀얼 Y축)
- [x] 커스텀 마커 (원형, 사각형)
- [x] 커스텀 툴팁
- [x] 범례 토글 기능
- [x] 범례 색상 변경
- [x] 범례 상태 지속성

#### UI 컴포넌트

- [x] Button, Input, Select, Textarea
- [x] Card, Modal, Alert
- [x] Header & Sidebar 네비게이션
- [x] 다크/라이트 모드 토글 (아이폰 스타일)
- [x] 로딩 상태 표시
- [x] 에러 처리 UI

#### 반응형 & 접근성

- [x] 모바일 최적화
- [x] 태블릿 최적화
- [x] 데스크톱 최적화
- [x] 터치 제스처 지원
- [x] 키보드 네비게이션
- [x] ARIA 레이블

## 🏗️ 아키텍쳐 특징

### Feature-First 구조

- 기능별로 모듈화된 코드 구조
- 각 feature는 독립적인 컴포넌트, 훅, 타입 포함
- 높은 응집도와 낮은 결합도

### 레이어드 아키텍쳐

```
┌─────────────────────────────────┐
│   Presentation Layer            │  ← React 컴포넌트, 페이지
├─────────────────────────────────┤
│   Business Logic Layer          │  ← Custom Hooks, 상태 관리
├─────────────────────────────────┤
│   Data Access Layer             │  ← API Client, Axios
└─────────────────────────────────┘
```

### 상태 관리 전략

#### 클라이언트 상태 (Zustand)

- 인증 상태 (`auth-store.ts`)
- 테마 설정 (`theme-store.ts`)
- 차트 범례 상태 (`chart-legend-store.ts`)

#### 서버 상태 (React Query)

- 게시글 데이터 (무한 쿼리)
- 차트 데이터 (캐싱 전략)
- 자동 리페칭 및 낙관적 업데이트

### 에러 처리 전략

1. **API 레벨**: Axios 인터셉터로 중앙 집중식 에러 처리
2. **React Query 레벨**: `throwOnError: false`로 mutation 안정성
3. **컴포넌트 레벨**: Error Boundary로 렌더링 에러 포착
4. **사용자 피드백**: Sonner Toast로 친화적인 에러 메시지

### 코드 품질

- **타입 안정성**: TypeScript + Zod로 런타임/컴파일타임 검증
- **재사용성**: 공통 UI 컴포넌트 라이브러리
- **일관성**: ESLint + Prettier로 코드 스타일 통일
- **성능**: React Query 캐싱, 무한 스크롤, 코드 스플리팅

### 보안

- HTTP-only 쿠키로 토큰 저장
- Next.js 미들웨어로 서버 사이드 인증
- XSS 방지 (금칙어 필터링)
- CSRF 토큰 (구현 가능)

## 📦 빌드 & 배포

### 프로덕션 빌드

```bash
npm run build
```

### 프로덕션 서버 실행

```bash
npm start
```

### Lint 실행

```bash
npm run lint
```

## 🔧 환경 변수 (옵션)

필요시 `.env.local` 파일을 생성하여 환경 변수를 설정할 수 있습니다:

```env
# API 베이스 URL (기본값: https://fe-hiring-rest-api.vercel.app)
NEXT_PUBLIC_API_URL=https://fe-hiring-rest-api.vercel.app

# 개발 모드 설정
NODE_ENV=development
```

> **참고**: 현재는 API URL이 코드에 하드코딩되어 있어 환경 변수 없이도 작동합니다.

## 🎨 주요 기술적 특징

### 1. 무한 스크롤 구현

```typescript
// React Query의 useInfiniteQuery 활용
const { data, fetchNextPage, hasNextPage, isFetchingNextPage } =
  useInfiniteQuery({
    queryKey: ["posts", filters],
    queryFn: ({ pageParam }) => fetchPosts(pageParam),
    getNextPageParam: (lastPage) => lastPage.nextCursor,
  });
```

### 2. 차트 범례 상태 관리

```typescript
// Zustand로 범례 상태 지속성
const { legendItems, toggleItem, updateColor } = useChartLegend(
  chartId,
  initialLegend
);
```

### 3. 타입 안전한 API 클라이언트

```typescript
// 타입 정의와 함께 API 호출
const response = await apiClient.post<Post>(API_ENDPOINTS.POSTS.BASE, data);
```

### 4. 반응형 디자인 전략

- **Mobile First**: 모바일부터 디자인 시작
- **Breakpoints**: sm (640px), md (768px), lg (1024px), xl (1280px)
- **Adaptive Components**: 화면 크기별 다른 UI (테이블 ↔ 카드)

### 5. 다크 모드 구현

```typescript
// Tailwind CSS의 dark: 프리픽스 활용
className = "bg-white dark:bg-gray-950";
```

## 📚 추가 문서

- [ARCHITECTURE.md](./ARCHITECTURE.md) - 상세한 아키텍쳐 설명
- [DEVELOPMENT.md](./DEVELOPMENT.md) - 개발 가이드 및 컨벤션
- [SETUP_COMPLETE.md](./SETUP_COMPLETE.md) - 초기 설정 체크리스트

## 🐛 알려진 이슈

현재 알려진 이슈가 없습니다. 버그 발견 시 이슈를 등록해주세요.

## 🙋‍♂️ 문의

프로젝트 관련 문의사항은 이슈를 등록해주세요.

---

## 🖥️ 배포 링크

https://directional-sigma.vercel.app/

**Made with ❤️ using Next.js 16 + TypeScript + Tailwind CSS**
