# 🏗️ 프로젝트 아키텍쳐

## 📁 디렉토리 구조

```
directional/
├── app/                          # Next.js App Router
│   ├── layout.tsx               # 루트 레이아웃 (QueryProvider 포함)
│   ├── page.tsx                 # 홈 페이지
│   ├── login/                   # 로그인 페이지
│   │   └── page.tsx
│   ├── posts/                   # 게시판 페이지
│   │   └── page.tsx
│   └── dashboard/               # 대시보드 페이지
│       └── page.tsx
│
├── src/
│   ├── components/              # 공통 컴포넌트
│   │   └── ui/                  # 기본 UI 컴포넌트
│   │       ├── Button/
│   │       ├── Input/
│   │       └── Modal/
│   │
│   ├── features/                # 기능별 모듈 (향후 구현)
│   │   ├── posts/               # 게시판 기능
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   └── types/
│   │   └── charts/              # 차트 기능
│   │       ├── components/
│   │       ├── hooks/
│   │       └── types/
│   │
│   ├── lib/                     # 핵심 라이브러리
│   │   ├── api/                 # API 클라이언트
│   │   │   ├── client.ts        # Axios 인스턴스
│   │   │   └── endpoints/       # API 엔드포인트
│   │   │       ├── auth.ts
│   │   │       ├── posts.ts
│   │   │       └── charts.ts
│   │   ├── store/               # Zustand 스토어
│   │   │   ├── auth-store.ts
│   │   │   └── chart-legend-store.ts
│   │   ├── providers/           # React 프로바이더
│   │   │   └── query-provider.tsx
│   │   └── utils/               # 유틸리티 함수
│   │       └── index.ts
│   │
│   ├── types/                   # TypeScript 타입 정의
│   │   ├── common.ts
│   │   ├── auth.ts
│   │   ├── post.ts
│   │   └── chart.ts
│   │
│   └── constants/               # 상수
│       ├── api.ts
│       ├── validation.ts
│       └── chart-colors.ts
│
└── package.json                 # 의존성
```

## 🛠️ 기술 스택

### 핵심

- **React**: 18+ (실제 19.2.0)
- **Next.js**: 16.0.5 (App Router)
- **TypeScript**: 5+

### 상태 관리

- **Zustand**: 클라이언트 상태 관리 (auth, chart legends)
- **React Query**: 서버 상태 관리 및 캐싱

### API & 통신

- **Axios**: HTTP 클라이언트 (인터셉터로 인증 자동화)
- **@tanstack/react-query**: 서버 상태 관리

### UI & 스타일

- **Tailwind CSS**: 유틸리티 우선 CSS
- **Recharts**: 차트 라이브러리
- **@tanstack/react-table**: 테이블 라이브러리

### 폼 & 검증

- **React Hook Form**: 폼 관리
- **Zod**: 스키마 검증

## 📐 아키텍쳐 패턴

### 1. 레이어드 아키텍쳐

```
Presentation Layer (컴포넌트)
        ↓
Business Logic Layer (훅, 스토어)
        ↓
Data Access Layer (API 클라이언트)
        ↓
External API
```

### 2. 상태 관리 전략

- **클라이언트 상태**: Zustand

  - 인증 상태 (토큰, 사용자 정보)
  - 차트 범례 상태 (색상, 가시성)

- **서버 상태**: React Query
  - 게시글 목록/상세
  - 차트 데이터
  - 캐싱, 재시도, 무효화 자동 처리

### 3. API 클라이언트 구조

```typescript
// Axios 인스턴스 + 인터셉터
apiClient
  ├── Request Interceptor: 토큰 자동 추가
  └── Response Interceptor: 에러 처리, 401 자동 리다이렉트

// 엔드포인트별 API 함수
authApi.login()
postsApi.getPosts()
chartsApi.getCoffeeConsumption()
```

### 4. 컴포넌트 구조

#### 게시판 (예정)

```
PostsPage
  ├── PostsTable (컨테이너)
  │   ├── TableToolbar (검색, 필터, 정렬)
  │   ├── DataTable (react-table)
  │   └── PostFormModal (CRUD)
  └── InfiniteScroll
```

#### 차트 (예정)

```
DashboardPage
  ├── ChartCard
  │   ├── ChartHeader
  │   ├── ChartBody
  │   │   └── [BarChart|DonutChart|StackedChart|MultiLineChart]
  │   └── CustomLegend
  │       ├── ColorPicker
  │       └── VisibilityToggle
```

## 🔐 인증 플로우

```
1. 사용자 로그인
   ↓
2. authApi.login() → 토큰 받기
   ↓
3. useAuthStore.login() → 토큰 저장 (localStorage + Zustand)
   ↓
4. 이후 모든 요청에 자동으로 토큰 포함 (Axios 인터셉터)
   ↓
5. 401 에러 시 자동 로그아웃 & 로그인 페이지 리다이렉트
```

## 📊 데이터 흐름 (React Query)

```
Component
  ↓ useQuery/useMutation
React Query
  ↓ fetcher function
API Client (Axios)
  ↓ HTTP Request
Backend API
  ↓ Response
React Query Cache
  ↓ auto-update
Component Re-render
```

## 🎯 핵심 기능 구현 계획

### 1. 게시판 (향후 구현)

- [ ] 무한 스크롤 (useInfiniteQuery)
- [ ] 검색/필터/정렬 (URL query params)
- [ ] 컬럼 리사이징 (react-table)
- [ ] 컬럼 숨김/보임
- [ ] CRUD 모달
- [ ] 금칙어 필터링

### 2. 차트 (향후 구현)

- [ ] 바 차트 & 도넛 차트
- [ ] 스택형 바/면적 차트
- [ ] 멀티라인 차트
- [ ] 범례 색상 변경
- [ ] 범례 토글 (데이터 숨김/보임)
- [ ] 커스텀 툴팁

## 📝 코딩 컨벤션

### 파일명

- 컴포넌트: PascalCase (예: `Button.tsx`)
- 유틸/훅: kebab-case (예: `use-posts.ts`)
- 타입: kebab-case (예: `post.ts`)

### Import 순서

1. React/Next.js
2. 외부 라이브러리
3. 내부 라이브러리 (@/lib)
4. 컴포넌트 (@/components)
5. 타입 (@/types)
6. 스타일

### 주석

- 파일 상단에 목적 주석 추가
- 복잡한 로직에는 설명 주석

## 🚀 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. 개발 서버 실행

```bash
npm run dev
```

### 3. API 문서 확인

https://fe-hiring-rest-api.vercel.app/docs

## 📦 주요 패키지

| 패키지                | 버전    | 용도            |
| --------------------- | ------- | --------------- |
| next                  | ^16.0.5 | 프레임워크      |
| react                 | ^19.2.0 | UI 라이브러리   |
| typescript            | ^5      | 타입 시스템     |
| zustand               | ^4.5.0  | 상태 관리       |
| @tanstack/react-query | ^5.0.0  | 서버 상태 관리  |
| axios                 | ^1.6.0  | HTTP 클라이언트 |
| recharts              | ^2.10.0 | 차트            |
| @tanstack/react-table | ^8.11.0 | 테이블          |
| react-hook-form       | ^7.49.0 | 폼 관리         |
| zod                   | ^3.22.0 | 스키마 검증     |

## 🎨 디자인 시스템

### 색상

- Primary: Blue (#3B82F6)
- Secondary: Gray
- Success: Green
- Danger: Red

### 간격

- Tailwind 기본 스케일 사용 (4px 단위)

### 타이포그래피

- Font: Geist Sans (기본)
- Headings: font-bold
- Body: font-normal

## 📄 다음 단계

1. ✅ 기본 구조 설정 완료
2. 🔄 게시판 컴포넌트 구현
3. 🔄 차트 컴포넌트 구현
4. 🔄 반응형 디자인 최적화
5. 🔄 테스트 추가
